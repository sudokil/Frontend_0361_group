if (условие 1) {
    вариант 1
} else if (условие 2) {
    вариант 2
} else if (условие 3) {
    вариант 3
} else {
    вариант если все условия НЕ выполнены
}

for (let i = 0 /* создаем счетчик с начальным значением */; i < answer.length /* сравнение */; i++ /* изменение счетчика */) {
    повторяемые действия
}

break

continue

while (условие) {
    повторяемые действия
}

do {
    повторяемые действия
} while (условие);

for ... of

for ... in


(...) ? (...) : (...)


switch (проверяемое_значение) {
    case значение1:
        действия1
        break;
    case значение2:
        действия2
    case значение3:
        break;
    case значение4:
        действия3
    default:
        действия_по_умолчанию
}

если проверяемое_значение == значение1 --> действия1, break
если проверяемое_значение == значение2 --> действия2, break
если проверяемое_значение == значение3 --> break
если проверяемое_значение == значение4 --> действия3, действия_по_умолчанию
иначе --> действия_по_умолчанию

